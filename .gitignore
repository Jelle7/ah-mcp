# Secrets and tokens — never commit these
.env
tokens.json
tokens.json.tmp
config.json
.github_token

# Compiled binaries
ah-mcp
ah-mcp.exe
ah-mcp.exe~
/dist/

# macOS
.DS_Store

# Go build artifacts
/vendor/
